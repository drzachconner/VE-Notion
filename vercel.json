{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    }
  ],
  "crons": [
    {
      "path": "/api/cron/post-everywhere",
      "schedule": "0 */3 * * *"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
